Server/MuchToDo/muchtodo-app
**Remove it from tracking** (this won't delete it from your computer, just from GitHub):
    ```bash
    git rm --cached Server/MuchToDo/muchtodo-app
    git commit -m "chore: remove binary from tracking"
    git push origin main
    ```

### What's next?
Now that the **Docker** portion is pushed, you've successfully:
*   [x] Containerized the Golang API.
*   [x] Linked it to MongoDB and Redis via Docker Compose.
*   [x] Fixed the environment variable mapping.
*   [x] Pushed everything to your Assessment Repo.

